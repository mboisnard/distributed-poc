spring:
  application:
    name: eureka

server:
  port: 8000

management:
  security:
    enabled: false

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false